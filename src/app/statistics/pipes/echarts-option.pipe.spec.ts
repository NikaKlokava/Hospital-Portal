import { EchartsOptionPipe } from './echarts-option.pipe';

describe('EchartsOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new EchartsOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
